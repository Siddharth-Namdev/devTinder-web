//export const BASE_URL = "/api";
export const BASE_URL =
  // location.hostname === "localhost" ? "http://localhost:7777" : "/api";
  location.hostname === "localhost" ? "http://dev-tinder-liard.vercel.app" : "/api";
